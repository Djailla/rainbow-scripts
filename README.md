Rainbow scripts - NAS OS App
==================================

Rainbow is the tools used to build NAS OS packages, here is an helper to ease the use.
Use `sudo rainbow-build` to run it in the NAS OS SDK.